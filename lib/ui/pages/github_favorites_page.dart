import 'package:flutter/material.dart';

class GithubFavoritesPage extends StatelessWidget {
  const GithubFavoritesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
